/* Generated by build.py — the four newest dispatches. */
(function () {
  var slot = document.querySelector("[data-posts]");
  if (!slot) return;
  slot.innerHTML = '<a class="dispatch rise" href="blog/ten-features-with-claude.html"><span class="dispatch__no">M-01</span><span><h3>I built a ten-feature Flutter app with an AI. Here is what it actually cost.</h3><p>The demo videos show a prompt and a finished app. What they leave out is the four hours I spent on a socket table, the crash that only happened on a real phone, and the accessibility bug the AI wrote and then caught.</p></span><span class="dispatch__date">2026-08-28</span></a><a class="dispatch rise" href="blog/offline-first-outbox.html"><span class="dispatch__no">M-02</span><span><h3>Offline-first sync in Flutter, and the bug that ate my queue</h3><p>An outbox is twenty lines. The interesting part is what happens on the fourth retry, when the server has moved on and the app has been killed twice.</p></span><span class="dispatch__date">2026-08-24</span></a><a class="dispatch rise" href="blog/ble-stream-lifecycle.html"><span class="dispatch__no">M-03</span><span><h3>BLE in Flutter: the reconnect bug you are going to ship</h3><p>Every packet arriving twice is not a radio problem. It is a subscription you forgot to cancel, and the third reconnect is where you find out.</p></span><span class="dispatch__date">2026-08-20</span></a><a class="dispatch rise" href="blog/add-to-app-pigeon.html"><span class="dispatch__no">M-04</span><span><h3>Add-to-app — one Flutter screen inside Kotlin and Swift</h3><p>Nobody rewrites twelve years of native code for one screen. The interesting question is what it costs to drop Flutter into an app that already exists — and the answer is one number.</p></span><span class="dispatch__date">2026-08-16</span></a>';
  // Reveal them: comic.js has already run its query by now.
  [].forEach.call(slot.querySelectorAll(".rise"), function (el, i) {
    el.style.setProperty("--d", (i * 70) + "ms");
    setTimeout(function () { el.classList.add("in"); }, 60);
  });
}());
